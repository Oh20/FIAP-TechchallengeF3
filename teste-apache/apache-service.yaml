apiVersion: v1
kind: Service
metadata:
  name: apache-test
  namespace: toggle-apps
  labels:
    app: apache-test
spec:
  type: ClusterIP
  selector:
    app: apache-test
  ports:
    - name: http
      port: 80
      targetPort: 80
