apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: toggle-apps
  labels:
    app: targeting-service
spec:
  type: ClusterIP
  selector:
    app: targeting-service
  ports:
    - name: http
      port: 80
      targetPort: 8003
