apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base do flag-service consumida pelo overlay em Infra/GitOps/prod/flag-service.
# Fora da base de proposito:
#   secret.example.yaml -> segredo real nao vai para o Git (vem do pipeline)
#   migration-job.yaml  -> migracao e operacao imperativa e ordenada, nao estado desejado
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
