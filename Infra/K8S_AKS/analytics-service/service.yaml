apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: toggle-apps
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 80
      targetPort: 8005
