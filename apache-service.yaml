apiVersion: v1
kind: Service
metadata:
  name: apache-test
  namespace: agrmudoufeladapulta
  labels:
    app: apache-test
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: apache-test
  ports:
    - name: http
      port: 80
      targetPort: 80

# teste apach