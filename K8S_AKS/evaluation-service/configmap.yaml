apiVersion: v1
kind: ConfigMap
metadata:
  name: evaluation-service-config
  namespace: toggle-apps
  labels:
    app: evaluation-service
data:
  PORT: "8004"
  FLAG_SERVICE_URL: "http://flag-service"
  TARGETING_SERVICE_URL: "http://targeting-service"
  SERVICE_BUS_QUEUE_NAME: "togglemasterqueue"
