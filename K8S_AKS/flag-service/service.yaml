apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: toggle-apps
  labels:
    app: flag-service
spec:
  type: ClusterIP
  selector:
    app: flag-service
  ports:
    - name: http
      port: 80
      targetPort: 8002
