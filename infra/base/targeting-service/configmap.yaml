apiVersion: v1
kind: ConfigMap
metadata:
  name: targeting-service-config
  namespace: toggle-apps
  labels:
    app: targeting-service
data:
  PORT: "8003"
  AUTH_SERVICE_URL: "http://auth-service.toggle-apps"
