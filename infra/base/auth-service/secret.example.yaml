apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: toggle-apps
  labels:
    app: auth-service
type: Opaque
stringData:
  DATABASE_URL: "postgres://USER:PASSWORD@HOST:5432/auth_db?sslmode=require"
  MASTER_KEY: "CHANGE_ME"
